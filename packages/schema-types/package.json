{
  "name": "@tiedaoyun/schema-types",
  "version": "0.1.0",
  "private": true,
  "description": "铁道云共享类型定义（数据模型 / 表单 / 列表 / 页面 / 流程 schema）",
  "license": "Apache-2.0",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsc -p tsconfig.json --watch"
  }
}
