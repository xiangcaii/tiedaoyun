{
  "name": "@tiedaoyun/utils",
  "version": "0.1.0",
  "private": true,
  "description": "铁道云共享工具函数（前后端通用，无运行时环境依赖）",
  "license": "Apache-2.0",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsc -p tsconfig.json --watch"
  }
}
