# Plan · M1: v0.1 MVP

| 项目 | 内容 |
| --- | --- |
| 里程碑 | M1 — v0.1 MVP |
| 创建日期 | 2026-07-31 |
| 关联 Spec | [HLD · 2026-07-31](../specs/2026-07-31-hld.md) |
| 关联路线图 | [ROADMAP.md](../../ROADMAP.md) |
| 任务粒度 | 30–60 min / task |
| 目标 | 跑通「数据模型 → 表单 → 列表 → 流程 → 页面」端到端 demo（请假申请），并交付可 Docker Compose 一键启动的单机部署 |

---

## 阶段 0 · 初始化

- [x] **T1**: 初始化 monorepo 工程脚手架
  - goal: 拉起 pnpm workspace + 根 package.json + tsconfig + ESLint + Prettier + Husky + lint-staged；包含 README.md、CHANGELOG.md、.gitignore、Makefile
  - files: `package.json`、`.gitignore`、`README.md`、`CHANGELOG.md`、`Makefile`、`tsconfig.base.json`、`pnpm-workspace.yaml`、`.eslintrc.cjs`、`.prettierrc`、`.husky/`
  - acceptance: `pnpm install` 成功；`pnpm lint` / `pnpm typecheck` 全绿；`Makefile` 含 `make dev / build / test / lint` 目标
  - spec: [HLD §3](../specs/2026-07-31-hld.md#3-仓库与目录结构)

- [x] **T2**: 搭建 Docker Compose 单机部署骨架
  - goal: 编排 postgres / redis / server / web / nginx / backup 6 个服务，含健康检查与数据卷
  - files: `deploy/docker-compose.yml`、`deploy/docker/server.Dockerfile`、`deploy/docker/web.Dockerfile`、`deploy/docker/nginx.conf`、`.env.example`
  - acceptance: `docker compose config` 校验通过；`docker compose up -d` 启动后 `curl localhost/healthz` 返回 200
  - spec: [HLD §12](../specs/2026-07-31-hld.md#12-部署与运维)

- [x] **T3**: 搭建 CI（GitHub Actions）
  - goal: PR 触发 lint / typecheck / 单测 / 构建镜像 4 个 job，main 分支推送 GHCR
  - files: `.github/workflows/ci.yml`、`.github/workflows/release.yml`
  - acceptance: 推 PR 后 Actions 4 个 job 全绿；镜像成功 push 到 GHCR（占位 owner 即可）
  - spec: [HLD §12.5](../specs/2026-07-31-hld.md#125-ci建议)

- [ ] **T4**: 建立共享 packages 骨架
  - goal: 创建 `schema-types` / `ui-kit` / `utils` 三个共享包，能在 web/mobile/server 引用
  - files: `packages/schema-types/package.json` + `src/index.ts`、`packages/ui-kit/package.json` + `src/index.tsx`、`packages/utils/package.json` + `src/index.ts`
  - acceptance: `pnpm -F @tiedaoyun/schema-types build` 成功；`apps/server` 引用 `@tiedaoyun/utils` 通过 typecheck
  - spec: [HLD §4.3](../specs/2026-07-31-hld.md#43-共享包)

---

## 阶段 1 · 服务端地基

- [ ] **T5**: 搭建 NestJS 服务骨架
  - goal: 启动 NestJS app，含 ConfigModule、全局 ValidationPipe、全局 ExceptionFilter、pino 日志、CORS、Helmet
  - files: `apps/server/src/main.ts`、`apps/server/src/app.module.ts`、`apps/server/src/config/`、`apps/server/src/common/`
  - acceptance: `pnpm -F @tiedaoyun/server start:dev` 启动成功；`curl localhost:3000/healthz` 返回 200
  - spec: [HLD §4.1](../specs/2026-07-31-hld.md#41-后端模块nestjs)、[§11](../specs/2026-07-31-hld.md#11-审计与可观测)

- [ ] **T6**: 接入 Prisma + 元数据 schema
  - goal: 落地 PRD HLD §5.3 元数据表 + 物理表命名约定，提供 PrismaService 与生成 migration 的脚本
  - files: `apps/server/prisma/schema.prisma`、`apps/server/src/infra/prisma/`、`scripts/generate-migration.ts`
  - acceptance: `pnpm prisma migrate dev` 成功生成首批迁移；`pnpm prisma studio` 可看到 21 张元数据表
  - spec: [HLD §5](../specs/2026-07-31-hld.md#5-数据架构)

- [ ] **T7**: 实现 Auth 模块（JWT）
  - goal: 账号密码登录 + 刷新 token + 当前用户查询
  - files: `apps/server/src/modules/auth/`、`apps/server/src/modules/user/`
  - acceptance: 单测覆盖登录成功 / 失败 / 过期；Postman / Vitest 集成测：登录后访问 `/api/v1/me` 返回用户
  - spec: [HLD §7.1](../specs/2026-07-31-hld.md#71-鉴权)

- [ ] **T8**: 实现 Workspace + Role + Member 模块
  - goal: 工作空间 CRUD、成员邀请/移除、角色绑定、权限点维护
  - files: `apps/server/src/modules/workspace/`、`apps/server/src/modules/role/`
  - acceptance: 单测覆盖加入/退出工作空间、角色分配；E2E：管理员邀请用户，被邀请者可访问工作空间
  - spec: [HLD §5.3](../specs/2026-07-31-hld.md#53-核心元数据表)、[§7.3](../specs/2026-07-31-hld.md#73-实现)

- [ ] **T9**: 实现 Permission 服务（功能 / 行 / 字段三级）
  - goal: `PermissionService.evaluate(user, resource, action, context)` + `PermissionGuard`，覆盖功能 / 行 / 字段三层
  - files: `apps/server/src/modules/permission/`、`apps/server/src/common/guards/`
  - acceptance: 单测覆盖内置角色策略 + 自定义策略 + 默认拒绝；用例：字段白名单生效、行级策略"我看本部门"生效
  - spec: [HLD §7](../specs/2026-07-31-hld.md#7-鉴权与权限)

---

## 阶段 2 · 核心域：数据模型

- [ ] **T10**: 数据模型模块：实体 / 字段 / 关系定义 CRUD
  - goal: 实体、字段、关系、索引、校验规则的元数据 CRUD
  - files: `apps/server/src/modules/data-model/`、`packages/schema-types/src/entity.ts`
  - acceptance: API 端到端可用：新建实体 → 加字段 → 加关系；单测覆盖唯一性、外键校验
  - spec: [HLD §5](../specs/2026-07-31-hld.md#5-数据架构)、[PRD §7.2.1](../../PRD.md#721-数据模型设计器)

- [ ] **T11**: 动态物理表生成器
  - goal: 保存实体时自动生成对应 Prisma 物理表 + 索引 + 外键 + 软删字段；启动时一致性自检
  - files: `apps/server/src/modules/data-model/migrator.ts`、`apps/server/src/infra/prisma/dynamic.ts`
  - acceptance: 新建实体后 PostgreSQL 中可见 `e_<app>__<entity>` 表；启动自检：手动 drop 一张物理表后启动能自动补回
  - spec: [HLD §5.4](../specs/2026-07-31-hld.md#54-动态表管理)

- [ ] **T12**: 影响分析 + 字段迁移策略
  - goal: 字段类型变更 / 删除时列出被引用位置（表单 / 列表 / 流程 / 页面），要求显式确认
  - files: `apps/server/src/modules/data-model/impact.ts`
  - acceptance: 用例：删除被表单引用的字段返回 409 + 引用清单；类型变更返回兼容性矩阵
  - spec: [PRD §7.2.1 边界与异常](../../PRD.md#721-数据模型设计器)

- [ ] **T13**: DataEngine 动态 CRUD 服务
  - goal: 对物理表封装 `query / create / update / delete / batch*`，强制走 PermissionGuard
  - files: `apps/server/src/modules/data-engine/`
  - acceptance: 单测覆盖批量写入、事务回滚、行级 / 字段级权限；用例：无权限字段在 `query` 返回中不出现
  - spec: [HLD §5.4](../specs/2026-07-31-hld.md#54-动态表管理)、[§6.3](../specs/2026-07-31-hld.md#63-关键端点v01)

---

## 阶段 3 · 核心域：表单 / 列表 / 页面

- [ ] **T14**: Form 定义 + 运行时 API
  - goal: 表单 schema 存 / 取 / 版本化；运行时 API 拉取 schema + 提交数据
  - files: `apps/server/src/modules/form/`、`packages/schema-types/src/form.ts`
  - acceptance: 提交一条带字段权限的记录后写 `audit_logs`；权限测试：无权字段不返回
  - spec: [PRD §7.2.2](../../PRD.md#722-表单设计器)、[HLD §6.3](../specs/2026-07-31-hld.md#63-关键端点v01)

- [ ] **T15**: List 定义 + 查询 API
  - goal: 列表定义存 / 取；查询接口支持筛选 / 排序 / 分页 / 列权限
  - files: `apps/server/src/modules/list/`、`packages/schema-types/src/list.ts`
  - acceptance: 10000 条以上数据分页时正确返回 + 提示筛缩；无权列不在响应中
  - spec: [PRD §7.2.3](../../PRD.md#723-列表与详情视图)

- [ ] **T16**: Page 定义 API
  - goal: 页面 schema 存 / 取；运行时 API 拉取页面 + 数据源
  - files: `apps/server/src/modules/page/`、`packages/schema-types/src/page.ts`
  - acceptance: 单测覆盖引用数据源 ≤ 20 / 嵌套 ≤ 10 限制；超限返回 400
  - spec: [PRD §7.2.5](../../PRD.md#725-页面--应用搭建器)

---

## 阶段 4 · 核心域：流程引擎

- [ ] **T17**: 流程定义 + 版本管理
  - goal: 流程 schema 存 / 取 / 版本化，支持 dev / test / prod 三态
  - files: `apps/server/src/modules/workflow/definition.ts`、`packages/schema-types/src/workflow.ts`
  - acceptance: 单测覆盖版本切换、回滚；E2E：发布正式版需 changelog，否则 400
  - spec: [PRD §7.2.8](../../PRD.md#728-应用发布与版本)

- [ ] **T18**: 流程状态机 + 节点执行器骨架
  - goal: 实现 `NodeExecutor` 接口 + 开始 / 审批 / 抄送 / 结束 4 类节点；`WorkflowInstance` 状态机
  - files: `apps/server/src/modules/workflow/engine/`、`apps/server/src/modules/workflow/executors/`
  - acceptance: 单测覆盖状态机不变量（单实例单 running 节点）；用例：审批通过后流转到下一节点
  - spec: [HLD §8](../specs/2026-07-31-hld.md#8-流程引擎架构)

- [ ] **T19**: 流程运行时 API：启动 / 待办 / 审批
  - goal: 启动流程 / 拉待办 / 通过 / 拒绝 / 转交 API + BullMQ 异步任务
  - files: `apps/server/src/modules/workflow/runtime/`、`apps/server/src/infra/queue/`
  - acceptance: 集成测：发起请假 → 主管审批 → 结束，全程 `workflow_logs` 完整可重放
  - spec: [PRD §7.2.4](../../PRD.md#724-流程--审批引擎)

- [ ] **T20**: 流程高级特性：会签 / 或签 / 回退 / 加签 / 子流程
  - goal: 实现 PRD 7.2.4 中列出的全部操作类型与子流程
  - files: `apps/server/src/modules/workflow/executors/`
  - acceptance: 单测每种操作各 1 例；E2E：会签全部通过才到下一节点
  - spec: [PRD §7.2.4 规则约束](../../PRD.md#724-流程--审批引擎)

---

## 阶段 5 · 核心域：连接器

- [ ] **T21**: Connector 框架 + HTTP / Webhook 实现
  - goal: `Connector` 接口、BullMQ 调度、脱敏日志、超时与重试
  - files: `apps/server/src/modules/connector/`
  - acceptance: 单测：HTTP 200 / 500 / 超时各 1 例；用例：失败 3 次后写 `connector_alerts`
  - spec: [HLD §10](../specs/2026-07-31-hld.md#10-连接器架构)、[PRD §7.2.7](../../PRD.md#727-集成与连接器)

- [ ] **T22**: 数据库连接器（MySQL / PostgreSQL SELECT）
  - goal: 数据库连接器，只允许 SELECT，写操作需显式开启
  - files: `apps/server/src/modules/connector/impl/db.ts`
  - acceptance: 单测：连接成功、SELECT 解析、SQL 注入拦截；写操作被拒
  - spec: [PRD §7.2.7 规则约束](../../PRD.md#727-集成与连接器)

- [ ] **T23**: 通知中心（邮件 / 短信 / 企微 抽象）
  - goal: NotifyService 抽象 + Email 实现 + WeCom 适配器接口（v0.1 仅接口，不接真实 API）
  - files: `apps/server/src/modules/notify/`
  - acceptance: 单测：Email 渲染 + 重试；WeCom 调用走 mock，验证接口契约
  - spec: [HLD §10.4](../specs/2026-07-31-hld.md#104-触发器)

---

## 阶段 6 · 横切：审计 / 文件 / 备份

- [ ] **T24**: Audit 模块 + 全局拦截器
  - goal: 审计事件枚举 + 全局拦截器 + 异步 BullMQ 写入 + 检索 API + 敏感字段脱敏
  - files: `apps/server/src/modules/audit/`、`apps/server/src/common/interceptors/audit.interceptor.ts`
  - acceptance: 用例：登录 / 数据变更 / 流程操作 / 连接器调用 4 类事件均能检索；身份证 / 手机号脱敏
  - spec: [HLD §11.1](../specs/2026-07-31-hld.md#111-审计)、[PRD §7.2.9](../../PRD.md#729-审计与日志)

- [ ] **T25**: File 模块（本地卷）
  - goal: 文件上传 / 下载 / 删除 / 鉴权；`StorageProvider` 接口 + Local 实现
  - files: `apps/server/src/modules/file/`、`apps/server/src/infra/storage/`
  - acceptance: 集成测：上传 5MB 通过 / 50MB 拒绝；下载鉴权失效返回 403
  - spec: [HLD §2 StorageProvider](../specs/2026-07-31-hld.md#2-技术栈决策)

- [ ] **T26**: 自动备份 + 恢复脚本
  - goal: `backup` 容器每日 `pg_dump` + 保留策略；提供 `restore.sh`
  - files: `deploy/backup/Dockerfile`、`deploy/backup/backup.sh`、`deploy/backup/restore.sh`
  - acceptance: 手动执行 `backup.sh` 生成 `.sql.gz`；`restore.sh` 能从备份恢复并写审计
  - spec: [HLD §12.4](../specs/2026-07-31-hld.md#124-备份)

---

## 阶段 7 · 前端：地基

- [ ] **T27**: 搭建 web 端骨架
  - goal: Vite + React + AntD + React Router + Zustand + React Query；含布局、主题、登录页骨架
  - files: `apps/web/`、`apps/web/src/main.tsx`、`apps/web/src/App.tsx`、`apps/web/src/routes/`
  - acceptance: `pnpm -F @tiedaoyun/web dev` 启动；未登录访问受保护路由跳转到登录页
  - spec: [HLD §4.2](../specs/2026-07-31-hld.md#42-前端模块)

- [ ] **T28**: OpenAPI 客户端自动生成
  - goal: 后端暴露 `/api/docs`，CI 跑 `openapi-typescript` 生成 `@tiedaoyun/api-client`
  - files: `packages/api-client/`、`apps/server/src/main.ts`（接入 Swagger）
  - acceptance: 后端启动后 `curl localhost:3000/api/docs` 返回 OpenAPI JSON；前端 `pnpm -F @tiedaoyun/api-client build` 成功
  - spec: [HLD §6.2](../specs/2026-07-31-hld.md#62-openapi-自动化)

- [ ] **T29**: 登录 + 工作空间选择 + 路由守卫
  - goal: 登录页 / 工作空间选择页 / 路由守卫 + token 刷新
  - files: `apps/web/src/pages/auth/`、`apps/web/src/pages/workspace/`、`apps/web/src/stores/auth.ts`
  - acceptance: E2E：登录成功 → 选工作空间 → 进入 `/apps` 列表页；token 过期自动刷新
  - spec: [HLD §6.1](../specs/2026-07-31-hld.md#61-设计原则)

---

## 阶段 8 · 前端：设计器

- [ ] **T30**: 数据模型设计器 UI
  - goal: 实体列表 / 字段编辑 / 关系编辑 / 索引定义 UI
  - files: `apps/web/src/pages/designer/data-model/`
  - acceptance: 用例：可视化新建「请假」实体 + 字段 + 与「员工」实体的关系；保存后服务端物理表生成
  - spec: [PRD §7.2.1](../../PRD.md#721-数据模型设计器)

- [ ] **T31**: 表单设计器（基于 formily）
  - goal: 拖拽组件、字段绑定、显隐规则、校验规则、字段权限配置
  - files: `packages/form-designer/`、`apps/web/src/pages/designer/form/`
  - acceptance: 用例：拖一个请假表单，配置 5 个字段、1 条显隐规则、1 条校验规则；保存后运行时能拉取
  - spec: [PRD §7.2.2](../../PRD.md#722-表单设计器)、[HLD §9](../specs/2026-07-31-hld.md#9-表单--页面--列表设计)

- [ ] **T32**: 列表设计器
  - goal: 列设置 / 筛选 / 排序 / 分组 / 视图类型（表格 / 看板 / 日历 / 甘特 / 树，v0.1 先做表格 + 看板）
  - files: `apps/web/src/pages/designer/list/`
  - acceptance: 用例：请假列表按状态分组切换为看板视图，列设置拖拽排序生效
  - spec: [PRD §7.2.3](../../PRD.md#723-列表与详情视图)

- [ ] **T33**: 流程设计器
  - goal: 节点拖拽 / 边 / 条件 / 审批人配置 / 触发器
  - files: `packages/workflow-render/src/designer/`、`apps/web/src/pages/designer/workflow/`
  - acceptance: 用例：拖出「开始 → 主管审批 → 结束」流程并保存
  - spec: [PRD §7.2.4](../../PRD.md#724-流程--审批引擎)

- [ ] **T34**: 页面设计器（dnd-kit + 自研组件树）
  - goal: 自由布局、组件树、事件动作、JS 增强位（受控）
  - files: `packages/page-designer/`、`apps/web/src/pages/designer/page/`
  - acceptance: 用例：搭一个"请假工作台"页：顶部统计卡片 + 中部列表 + 侧边快捷入口；保存可预览
  - spec: [PRD §7.2.5](../../PRD.md#725-页面--应用搭建器)

---

## 阶段 9 · 前端：运行时

- [ ] **T35**: 表单运行时 + 提交
  - goal: `form-render` 渲染表单 / 字段权限生效 / 提交后跳列表
  - files: `packages/form-render/`、`apps/web/src/pages/runtime/form/`
  - acceptance: E2E：以"员工"账号提交请假单 → 主管待办出现新任务
  - spec: [PRD §7.2.2](../../PRD.md#722-表单设计器)

- [ ] **T36**: 列表运行时 + 详情抽屉
  - goal: 列表查询、筛选、分页、详情抽屉、行内编辑
  - files: `apps/web/src/pages/runtime/list/`
  - acceptance: E2E：列表筛选"未审批" → 打开详情 → 通过审批 → 列表自动刷新
  - spec: [PRD §7.2.3](../../PRD.md#723-列表与详情视图)

- [ ] **T37**: 页面运行时
  - goal: 渲染 page schema / 数据源绑定 / 动作事件
  - files: `packages/page-render/`、`apps/web/src/pages/runtime/page/`
  - acceptance: E2E：访问工作台页，数据源卡片显示正确统计
  - spec: [PRD §7.2.5](../../PRD.md#725-页面--应用搭建器)

- [ ] **T38**: 审批面板 + 我的待办
  - goal: 待办列表 / 通过 / 拒绝 / 转交 / 历史
  - files: `apps/web/src/pages/runtime/approval/`
  - acceptance: E2E：主管看到待办 → 通过 → 申请人状态变为"已通过"
  - spec: [PRD §7.2.4](../../PRD.md#724-流程--审批引擎)

---

## 阶段 10 · 移动端 H5

- [ ] **T39**: 移动端 shell + 表单运行时
  - goal: 移动端壳工程、扫码 / 定位拍照组件、IndexedDB 离线暂存
  - files: `apps/mobile/`、`apps/mobile/src/pages/form/`
  - acceptance: 模拟器断网提交一条表单 → 恢复后自动同步；服务器收到该条记录
  - spec: [PRD §7.2.10](../../PRD.md#7210-移动端)

- [ ] **T40**: 移动端审批面板
  - goal: 卡片式审批 / 一键通过 / 拒绝 / 转交
  - files: `apps/mobile/src/pages/approval/`
  - acceptance: E2E（移动视口）：主管在手机上完成审批
  - spec: [PRD §7.2.10](../../PRD.md#7210-移动端)

---

## 阶段 11 · 收尾

- [ ] **T41**: 首次启动向导
  - goal: 检测到未初始化时引导设置管理员账号 / 站点名 / logo
  - files: `apps/web/src/pages/setup/`、`apps/server/src/modules/setup/`
  - acceptance: 全新 `docker compose up` 后访问 `localhost` 进入向导；完成后正常进入登录
  - spec: [HLD §12.2](../specs/2026-07-31-hld.md#122-首次启动向导)

- [ ] **T42**: 种子数据 + Demo「请假申请」端到端打通
  - goal: `pnpm seed` 创建工作空间 + 角色 + 员工 + 审批流 + 请假应用模板
  - files: `scripts/seed.ts`、请假的 entity/form/list/workflow/page 定义 JSON
  - acceptance: `pnpm seed && docker compose up -d` 后用户按 README 即可走完"提交 → 审批 → 完成"
  - spec: [HLD §1 验证闭环](../specs/2026-07-31-hld.md#1-目标与非目标)

- [ ] **T43**: E2E 关键链路 + 文档完善
  - goal: Playwright 跑「登录 → 建数据 → 提交 → 审批 → 列表看到」端到端；完善 README / CONTRIBUTING / Quick Start
  - files: `e2e/`、`README.md`、`CONTRIBUTING.md`、`docs/quickstart.md`
  - acceptance: `pnpm e2e` 全绿；按 README 30 分钟内可在空白机器上完成部署
  - spec: [PRD §11](../../PRD.md#11-验收标准prd-级)、[HLD §13 性能基线](../specs/2026-07-31-hld.md#131-mvp-目标与-prd-84-对齐)

---

## 并行 / 依赖提示

- 阶段 0 全部串行。
- 阶段 1（T5–T9）大体串行，但 T8 与 T7 可部分并行。
- 阶段 2–6 可在阶段 1 完成后大量并行：T10–T13、T14–T16、T17–T20、T21–T23、T24–T26。
- 阶段 7（T27–T29）依赖阶段 0；可与阶段 1 后期并行。
- 阶段 8 全部依赖阶段 2 完成 entity CRUD（T10）以及阶段 7 完成 web 骨架。
- 阶段 9 依赖阶段 3 + 阶段 4 + 阶段 7。
- 阶段 10 依赖阶段 9 抽出的 form-render / 通用组件。
- 阶段 11 收尾，依赖前面所有阶段。

> 实际执行时由 `subagents` 阶段做更细的 2–5 min 拆解与依赖图调度。

---

## 验收（M1 自身）

1. 全部 43 个任务勾选完毕。
2. `pnpm lint && pnpm typecheck && pnpm test && pnpm build` 全绿。
3. `pnpm e2e` 跑通请假申请端到端。
4. 全新机器按 README 30 分钟内可完成 `docker compose up -d` → 提交 → 审批闭环。
5. 性能基线：首屏 ≤ 1.5s、列表查询 ≤ 1s、审批 API ≤ 500ms（P95，开发者本机参考）。
6. `docs/CHANGELOG.md` 记录 v0.1.0 首版。
